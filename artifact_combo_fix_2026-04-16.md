# ComboBox 修复 - 2026-04-16 08:45

## 问题
下拉框改成白色后，文字"视频" / "仅音频" 看不清了，颜色和背景重叠。

## 原因
`color: #1F1F1F`（接近黑色的深灰）在白色背景上对比度不够，或者 Qt 样式表没有正确应用到关闭状态下的 QComboBox。

## 修复
- 文字颜色：`#1F1F1F` → `#000000`（纯黑，对比度更强）
- 下拉列表文字：`#1F1F1F` → `#000000`
- 新增 `font-family` 声明，明确指定字体
- 确保 QSS 选择器精确匹配所有状态

## 验证
语法检查通过 ✅
